/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestMorgan from "nest-morgan";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { StartStopPlaceService } from "../startStopPlace.service";
import { StartStopPlaceCreateInput } from "./StartStopPlaceCreateInput";
import { StartStopPlaceWhereInput } from "./StartStopPlaceWhereInput";
import { StartStopPlaceWhereUniqueInput } from "./StartStopPlaceWhereUniqueInput";
import { StartStopPlaceFindManyArgs } from "./StartStopPlaceFindManyArgs";
import { StartStopPlaceUpdateInput } from "./StartStopPlaceUpdateInput";
import { StartStopPlace } from "./StartStopPlace";
@swagger.ApiBearerAuth()
export class StartStopPlaceControllerBase {
  constructor(
    protected readonly service: StartStopPlaceService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post()
  @nestAccessControl.UseRoles({
    resource: "StartStopPlace",
    action: "create",
    possession: "any",
  })
  @swagger.ApiCreatedResponse({ type: StartStopPlace })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: StartStopPlaceCreateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<StartStopPlace> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "StartStopPlace",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"StartStopPlace"} creation is forbidden for roles: ${roles}`
      );
    }
    return await this.service.create({
      data: {
        ...data,

        city: data.city
          ? {
              connect: data.city,
            }
          : undefined,
      },
      select: {
        city: {
          select: {
            id: true,
          },
        },

        closesAt: true,
        createdAt: true,
        geolocation: true,
        id: true,
        lastAvailable: true,
        name: true,
        opensAt: true,
        typeOfPlace: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get()
  @nestAccessControl.UseRoles({
    resource: "StartStopPlace",
    action: "read",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: [StartStopPlace] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(StartStopPlaceFindManyArgs)
  async findMany(
    @common.Req() request: Request,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<StartStopPlace[]> {
    const args = plainToClass(StartStopPlaceFindManyArgs, request.query);

    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "StartStopPlace",
    });
    const results = await this.service.findMany({
      ...args,
      select: {
        city: {
          select: {
            id: true,
          },
        },

        closesAt: true,
        createdAt: true,
        geolocation: true,
        id: true,
        lastAvailable: true,
        name: true,
        opensAt: true,
        typeOfPlace: true,
        updatedAt: true,
      },
    });
    return results.map((result) => permission.filter(result));
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id")
  @nestAccessControl.UseRoles({
    resource: "StartStopPlace",
    action: "read",
    possession: "own",
  })
  @swagger.ApiOkResponse({ type: StartStopPlace })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: StartStopPlaceWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<StartStopPlace | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "StartStopPlace",
    });
    const result = await this.service.findOne({
      where: params,
      select: {
        city: {
          select: {
            id: true,
          },
        },

        closesAt: true,
        createdAt: true,
        geolocation: true,
        id: true,
        lastAvailable: true,
        name: true,
        opensAt: true,
        typeOfPlace: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return permission.filter(result);
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id")
  @nestAccessControl.UseRoles({
    resource: "StartStopPlace",
    action: "update",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: StartStopPlace })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: StartStopPlaceWhereUniqueInput,
    @common.Body()
    data: StartStopPlaceUpdateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<StartStopPlace | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "StartStopPlace",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"StartStopPlace"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          city: data.city
            ? {
                connect: data.city,
              }
            : undefined,
        },
        select: {
          city: {
            select: {
              id: true,
            },
          },

          closesAt: true,
          createdAt: true,
          geolocation: true,
          id: true,
          lastAvailable: true,
          name: true,
          opensAt: true,
          typeOfPlace: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id")
  @nestAccessControl.UseRoles({
    resource: "StartStopPlace",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: StartStopPlace })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: StartStopPlaceWhereUniqueInput
  ): Promise<StartStopPlace | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          city: {
            select: {
              id: true,
            },
          },

          closesAt: true,
          createdAt: true,
          geolocation: true,
          id: true,
          lastAvailable: true,
          name: true,
          opensAt: true,
          typeOfPlace: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
